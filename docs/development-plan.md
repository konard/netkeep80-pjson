# План разработки pjson

## Фаза 1: Инфраструктура проекта (Текущая)

**Цель:** Создать структуру проекта, фреймворк требований и CI-пайплайн.

- [x] Создать JSON-схему требований с поддержкой трассировки
- [x] Определить начальные требования (бизнес, заинтересованные стороны, функциональные, нефункциональные, ограничения)
- [x] Создать скрипты валидации требований
- [x] Настроить CI-пайплайн для валидации требований
- [x] Написать документацию по архитектуре
- [x] Написать README.md

## Фаза 2: Миграция основной библиотеки

**Цель:** Мигрировать основную функциональность pjson из BinDiffSynchronizer, устраняя архитектурное дублирование.

### 2.1 Интеграция с pmm
- [ ] Добавить pmm как зависимость (git submodule или single-header include)
- [ ] Определить конфигурацию pmm для pjson (трейты адресов, бэкенд хранения, политика блокировок)
- [ ] Убедиться, что pmm компилируется в среде сборки pjson

### 2.2 Модель узлов
- [ ] Мигрировать `pjson_node.h` — систему типов JSON-значений
- [ ] Заменить `pjson_pool_pmm` (пул-аллокатор) на прямое выделение через pmm
- [ ] Убедиться, что структура узла тривиально копируема (static_assert)
- [ ] Мигрировать функции мутации узлов (set, push_back, insert)
- [ ] Мигрировать итераторы (array, object)

### 2.3 Персистентные контейнеры
- [ ] Заменить `pmap_pmm` (сортированный массив) на нативную `pmap` pmm (AVL-дерево)
- [ ] Использовать `parray` pmm для JSON-массивов и бинарных данных
- [ ] Использовать `pstring` pmm для изменяемых строковых значений
- [ ] Использовать `pstringview` pmm для интернированных строковых ключей и путей $ref

### 2.4 API базы данных
- [ ] Мигрировать адресацию по пути (`/a/b/0/c`)
- [ ] Мигрировать CRUD-операции (get, put, erase)
- [ ] Мигрировать разрешение $ref
- [ ] Мигрировать API метрик

### 2.5 Кодек
- [ ] Мигрировать JSON-парсер
- [ ] Мигрировать JSON-сериализатор
- [ ] Мигрировать поддержку расширения $base64
- [ ] Мигрировать поддержку расширения $ref

## Фаза 3: Тестирование

**Цель:** Портировать и расширить набор тестов.

- [ ] Портировать юнит-тесты из BinDiffSynchronizer
- [ ] Добавить тесты для прямого выделения через pmm (без пула)
- [ ] Добавить тесты для pmap на основе AVL-дерева (замена сортированного массива)
- [ ] Добавить тесты на сохранение-загрузку (save, load, verify)
- [ ] Добавить бенчмарки производительности
- [ ] Настроить CI-пайплайн для C++ (сборка CMake + CTest)

## Фаза 4: Доработка API

**Цель:** Отполировать API для внешнего использования.

- [ ] Спроектировать API-поверхность, совместимую с nlohmann::json, где это применимо
- [ ] Добавить удобные конструкторы и операторы
- [ ] Добавить обработку ошибок (исключения или коды ошибок, TBD)
- [ ] Сгенерировать single-header вариант
- [ ] Написать справочную документацию по API
- [ ] Добавить примеры использования

## Фаза 5: Продвинутые возможности

**Цель:** Добавить возможности, выходящие за рамки прототипа.

- [ ] Соответствие RFC 6901 JSON Pointer (экранирование `/` и `~` в ключах)
- [ ] Поддержка потокобезопасности (настраиваемая политика блокировок из pmm)
- [ ] Очистка интернирования строк (сборка мусора для неиспользуемых интернированных строк)
- [ ] Несколько экземпляров БД в одном процессе
- [ ] Интеграция бинарной синхронизации различий (из BinDiffSynchronizer)

## Фаза 6: СУБД-возможности (pjson_db)

**Цель:** Расширить pjson до персистентной СУБД.

### 6.1 Выделенные AVL-деревья для типов узлов
- [ ] Назначить фиксированные идентификаторы типов AVL-деревьев для каждого типа узла pjson (FR-009)
- [ ] Реализовать итераторы по типизированным AVL-деревьям (FR-010)
- [ ] Проверить, что K в pmap\<K,V\> всегда равен pstringview в контексте pjson

### 6.2 Индексация и поиск
- [ ] Реализовать индексацию данных по JSON-путям (FR-011)
- [ ] Реализовать полнотекстовый поиск по pstringview (интернированные строки)
- [ ] Реализовать полнотекстовый поиск по pstring (строковые значения)

### 6.3 Контроль доступа
- [ ] Реализовать разграничение прав доступа на уровне узлов (FR-012)
- [ ] Реализовать наследование прав доступа по ветвям JSON-дерева

### 6.4 Восстановление данных
- [ ] Реализовать восстановление данных через обход типизированных AVL-деревьев (FR-013)
- [ ] Реализовать отчёт о восстановлении (количество восстановленных/потерянных узлов по типам)

### 6.5 pjsonRVM
- [ ] Спроектировать виртуальную машину pjsonRVM по аналогии с jsonRVM

## Зависимости

| Зависимость | Версия | Назначение |
|-------------|--------|------------|
| [pmm](https://github.com/netkeep80/PersistMemoryManager) | latest | Управление персистентной памятью |
| Компилятор C++20 | GCC 12+ / Clang 15+ / MSVC 19.30+ | Стандарт языка |
| CMake | 3.20+ | Система сборки |
| Catch2 | v3 | Фреймворк тестирования |
