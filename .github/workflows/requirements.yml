name: Валидация требований

on:
  push:
    paths:
      - 'requirements/**'
      - 'scripts/validate-requirements.js'
  pull_request:
    paths:
      - 'requirements/**'
      - 'scripts/validate-requirements.js'

jobs:
  validate:
    name: Валидация требований
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Настройка Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Валидация требований
        run: node scripts/validate-requirements.js
